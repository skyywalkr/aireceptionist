[general]
enabled = yes
pretty = yes

[asterisk]
[ari]
[api]

; Create a user section with matching username/password for ARI

[asterisk]
type = user
read_only = no
password = secret

; Then in /etc/asterisk/ari.conf map an app name to your application (ai_receptionist) if needed
; The `app` used in our Flask app is `ai_receptionist` by default.
